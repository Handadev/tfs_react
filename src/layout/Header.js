import React from "react";

const Header = () => {
    return (
        <header>
            <div>
                <h1>일단 헤더</h1>
            </div>
            <button type="button" className="btn btn-success">
                일단 버튼
            </button>
        </header>
    )
};

export default Header;